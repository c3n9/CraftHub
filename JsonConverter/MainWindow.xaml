<Window x:Class="JsonConverter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JsonConverter"
        mc:Ignorable="d"
        Title="MainWindow" MinHeight="450" MinWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Menu Grid.ColumnSpan="2">
            <MenuItem Header="File">
                <MenuItem Header="Import class"
                          x:Name="MIImportClass"
                          Click="MIImportClass_Click" />
                <MenuItem Header="Import json file"
                          x:Name="MIImportJsonFile"
                          Click="MIImportJsonFile_Click" />
            </MenuItem>
            <MenuItem Header="Options" />
        </Menu>
        <DataGrid x:Name="DGJsonData"
                  Grid.Row="1"
                  Grid.Column="1"
                  ColumnWidth="auto"/>
        <DataGrid x:Name="DGProperties"
                  IsReadOnly="True" 
                  Grid.Row="1"/>
    </Grid>
</Window>
