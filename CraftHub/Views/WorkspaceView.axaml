<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:CraftHub.ViewModels"
             xmlns:models="using:CraftHub.Models"
             xmlns:dg="clr-namespace:Avalonia.Controls;assembly=Avalonia.Controls.DataGrid"
             x:Class="CraftHub.Views.WorkspaceView"
             x:DataType="vm:WorkspaceViewModel">

    <Grid RowDefinitions="Auto,Auto,Auto,*">

        <!-- Toolbar Row 1: Actions -->
        <Border Grid.Row="0" Background="{DynamicResource SurfaceBackground}" Padding="8,6"
                CornerRadius="8" Margin="8,8,8,0">
            <StackPanel Orientation="Horizontal" Spacing="4">
                <Button Classes="toolbar-btn" Content="📥 Import JSON"
                        Command="{Binding ImportJsonCommand}"
                        ToolTip.Tip="Import data from JSON file" />
                <Button Classes="toolbar-btn" Content="📤 Export JSON"
                        Command="{Binding ExportJsonCommand}"
                        ToolTip.Tip="Export data to JSON file" />

                <Border Width="1" Background="{DynamicResource BorderLight}" Margin="6,4" />

                <Button Classes="toolbar-btn" Content="📄 Import Class"
                        Command="{Binding ImportClassCommand}"
                        ToolTip.Tip="Import schema from C# class file" />
                <Button Classes="toolbar-btn" Content="💾 Export Class"
                        Command="{Binding ExportClassCommand}"
                        ToolTip.Tip="Export schema as C# class file" />

                <Border Width="1" Background="{DynamicResource BorderLight}" Margin="6,4" />

                <Button Classes="toolbar-btn" Content="➕ Row"
                        Command="{Binding AddRowCommand}"
                        ToolTip.Tip="Add new data row" />
                <Button Classes="toolbar-btn" Content="🗑 Row"
                        Command="{Binding RemoveRowsCommand}"
                        CommandParameter="{Binding #DataGrid.SelectedItems}"
                        ToolTip.Tip="Remove selected rows" />

                <Border Width="1" Background="{DynamicResource BorderLight}" Margin="6,4" />
                
                <TextBox Text="{Binding SearchQuery}" Watermark="Search by value..." 
                         Width="170" VerticalAlignment="Center" CornerRadius="6" FontSize="13" />
            </StackPanel>
        </Border>

        <!-- Toolbar Row 2: Add Property -->
        <Border Grid.Row="1" Background="{DynamicResource SurfaceBackground}" Padding="8,6"
                CornerRadius="8" Margin="8,6,8,0">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock Text="Add Property:" VerticalAlignment="Center"
                           Foreground="{DynamicResource TextMuted}" FontSize="13" />

                <TextBox Text="{Binding PropertyName}"
                         Watermark="Property name"
                         Width="200" FontSize="14"
                         x:Name="TBPropertyName"
                         CornerRadius="6" />

                <ComboBox ItemsSource="{Binding AvailableTypes}"
                          SelectedItem="{Binding SelectedType}"
                          Width="140" FontSize="14"
                          Height="{Binding #TBPropertyName.Bounds.Height}"
                          CornerRadius="6" />

                <ComboBox ItemsSource="{Binding AvailableTypes}"
                          SelectedItem="{Binding SelectedArrayElementType}"
                          IsVisible="{Binding IsArrayTypeSelected}"
                          Height="{Binding #TBPropertyName.Bounds.Height}"
                          Width="140" FontSize="14"
                          CornerRadius="6" />

                <Button Classes="accent-btn" Content="+ Add"
                        Command="{Binding AddPropertyCommand}" />
            </StackPanel>
        </Border>

        <!-- DataGrid -->
        <DataGrid Grid.Row="3"
                  x:Name="DataGrid"
                  ItemsSource="{Binding Rows}"
                  SelectedItem="{Binding SelectedRow}"
                  AutoGenerateColumns="False"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  SelectionMode="Extended"
                  IsReadOnly="False"
                  Margin="10"
                  FontSize="14"
                  Background="{DynamicResource ElevatedBackground}"
                  RowBackground="{DynamicResource ElevatedBackground}">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Duplicate Row(s)" 
                              Command="{Binding DuplicateRowsCommand}" 
                              CommandParameter="{Binding #DataGrid.SelectedItems}" />
                    <MenuItem Header="Copy Row(s) to JSON" 
                              Command="{Binding CopyRowsToJsonCommand}" 
                              CommandParameter="{Binding #DataGrid.SelectedItems}" />
                    <Separator />
                    <MenuItem Header="Remove Row(s)" 
                              Command="{Binding RemoveRowsCommand}" 
                              CommandParameter="{Binding #DataGrid.SelectedItems}"
                              Foreground="{DynamicResource DangerColor}" />
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>

        <!-- Schema / Status bar -->
        <Border Grid.Row="2" Background="{DynamicResource ElevatedBackground}" Padding="8,0"
                Margin="8,6,8,8" CornerRadius="8">
            <Grid ColumnDefinitions="*,Auto">
                <Grid ColumnDefinitions="Auto,*" Grid.Column="0">
                    <TextBlock Grid.Column="0" Text="Schema:" FontSize="12" Foreground="{DynamicResource TextMuted}"
                               VerticalAlignment="Center" Margin="0,0,6,0" />
                    <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Margin="0,0,8,0">
                        <ItemsControl ItemsSource="{Binding Properties}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="4"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type models:JsonPropertyDefinition}">
                                    <Border Background="{DynamicResource BorderLight}" CornerRadius="4"
                                            Padding="8,3" Cursor="Hand">
                                        <StackPanel Orientation="Horizontal" Spacing="4">
                                            <TextBlock FontSize="12" Foreground="{DynamicResource TextSecondary}">
                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="{}{0}: {1}">
                                                        <Binding Path="Name" />
                                                        <Binding Path="FieldType" />
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>
                                            <Button Classes="close-tab" Content="✕"
                                                    FontSize="10"
                                                    Command="{Binding $parent[UserControl].DataContext.RemovePropertyCommand}"
                                                    CommandParameter="{Binding}" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
